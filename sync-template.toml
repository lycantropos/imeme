[telegram]
api_id = 12345 # get your own `api_id` from https://my.telegram.org/apps
api_hash = "1234567890abcdef1234567890abcdef" # get your own `api_hash` from https://my.telegram.org/apps
peers = [
    # Saved messages
    "me",
    # Du Rove's Channel handle
    "durov",
    # Du Rove's Channel id
    -1001006503122,
    # Telegram service notifications
    777000,
]
# defines default languages for OCR by their ISO 639-1:2002 codes
default_languages = ["en", "ru"]
